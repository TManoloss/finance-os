import 'package:flutter/material.dart';

/// Paleta dark premium do Finance OS mobile.
class AppColors {
  static const background = Color(0xFF000000);
  static const surface = Color(0xFF121212);
  static const elevated = Color(0xFF1B1B1D);
  static const border = Color(0xFF2A2A2E);

  static const textPrimary = Color(0xFFF4F4F5);
  static const textSecondary = Color(0xFF919198);

  static const primary = Color(0xFF8B7CFF);
  static const success = Color(0xFF35C98B);
  static const danger = Color(0xFFFF6B6B);
  static const warning = Color(0xFFF5A524);

  // ── Dark Mode (referência) ────────────────────────────────────────────────
  static const darkBackground = Color(0xFF0A0A0F);
  static const darkSurface = Color(0xFF111118);
  static const darkElevated = Color(0xFF1A1A24);
  static const darkBorder = Color(0xFFFFFFFF);
  static const darkPrimary = Color(0xFF7C6FFF);
  static const darkSuccess = Color(0xFF4ECDC4);
}
